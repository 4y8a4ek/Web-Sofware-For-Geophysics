global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'fastapi'
    static_configs:
      - targets: ['api:8000']    # имя сервиса из docker-compose

  - job_name: 'worker'
    static_configs:
      - targets: ['worker:9000']